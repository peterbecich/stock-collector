module SetupLib where

import Util.LoadStocks
import Types.Exchange.Psql

-- setup :: IO ()
-- setup = do
--   insertExchangeIO nasdaq
--   insertExchangeIO nyse
--   insertExchangeIO amex
  
--   nasdaqRows <- insertNASDAQ
--   putStrLn $ "inserted " ++ (show nasdaqRows) ++ " stocks from NASDAQ"
  
--   nyseRows <- insertNYSE
--   putStrLn $ "inserted " ++ (show nyseRows) ++ " stocks from NYSE"
  
--   amexRows <- insertAMEX
--   putStrLn $ "inserted " ++ (show amexRows) ++ " stocks from AMEX"
  

  
